========================
Sunday, February 1, 2015
========================

Change `title` of your `Site` to `verbose_name`
===============================================

Stop using `title`. Use :attr:`verbose_name` instead.
